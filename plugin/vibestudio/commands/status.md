---
description: Show the vibestudio link status of this session
---

Run `vibestudio claude status` and summarize the result for the user: the tier
(0 = paired CLI only — polling participation via `vibestudio channel
send/history`; 1 = linked agent via the plugin; 2 = launched from the
workspace terminal), whether the vessel is attached, the bound context and
conversation, and what capability is missing at this tier plus how to get it.

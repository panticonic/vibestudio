{
  "name": "@vibestudio/webhook-relay",
  "private": true,
  "scripts": {
    "predev": "node build-website-assets.mjs",
    "dev": "wrangler dev",
    "predeploy": "node build-website-assets.mjs",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "typecheck:cloudflare": "tsc --noEmit",
    "test": "cd ../.. && vitest run apps/webhook-relay/src"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260730.1",
    "typescript": "7.0.2",
    "wrangler": "4.116.0"
  }
}

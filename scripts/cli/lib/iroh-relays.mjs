/**
 * Ordered public Iroh relay topology shipped by Vibestudio. These are public
 * n0 relays, not Vibestudio-operated infrastructure, so a default production
 * deployment does not depend on a private relay service.
 */
export const DEFAULT_IROH_RELAYS = Object.freeze([
  "https://use1-1.relay.n0.iroh.link/",
  "https://euc1-1.relay.n0.iroh.link/",
]);
